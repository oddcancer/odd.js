(function (odd) {
    var IM = odd.IM,

        State = {
            INITIALIZED: 'initialized',
            CONNECTED: 'connected',
            CLOSING: 'closing',
            CLOSED: 'closed',
        };

    IM.State = State;
})(odd);

